apiVersion: v1
kind: ConfigMap

metadata:
  name: flask-config

data:
  DB_HOST: "rds-endpoint.ap-south-1.rds.amazonaws.com"
  DB_NAME: "flaskapp"
  DB_PORT: "5432"
  APP_ENV: "production"
  LOG_LEVEL: "INFO"
